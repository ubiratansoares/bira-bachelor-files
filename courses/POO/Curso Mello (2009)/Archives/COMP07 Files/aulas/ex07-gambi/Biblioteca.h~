#include <iostream>

using namespace std;

class Biblioteca {
	private:
		VectorLivro *livros;
		VectorUser * users;
	public:
		// sobre a biblioteca
		void menu();
		void auth();
		void search();

		// Livros
		void add();
		void del();
		void list();

		// Users
		void create();
		void remove();
		void show();
};
